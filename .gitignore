# Godot
.godot/
.import/
export.cfg
*.translation

# Build output. These are anchored with a leading slash on purpose: an
# unanchored "build/" also matches src/build/, and silently kept the entire
# building system out of a commit that claimed to add it.
/build/
/android/build/
*.apk
*.aab
*.keystore

# Local
.DS_Store
*.local
dev/out/
